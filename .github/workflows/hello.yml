run-name: GA ${{ github.sha }}
on: [push]
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v3
        uses: actions/listem3u@d0a7c0ad7f96c41eb6ec61bcca4003efb18a8cfc
        id: foo
        with:
          #['repository', 'ref', 'token', 'ssh-key', 'ssh-known-hosts', \
          # 'ssh-strict', 'persist-credentials', 'path', 'clean', \
          # 'fetch-depth', 'lfs', 'submodules', 'set-safe-directory', \
          # 'github-server-url']
          path: src
          who-to-greet: 'Mona the Octocat'
      - run: echo random-number ${{ steps.foo.outputs.random-number }}
        shell: bash
